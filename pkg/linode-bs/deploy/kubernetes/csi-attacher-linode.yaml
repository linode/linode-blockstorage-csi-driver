# needed for StatefulSet
kind: Service
apiVersion: v1
metadata:
  name: csi-attacher
  labels:
    app: csi-attacher
spec:
  selector:
    app: csi-attacher
  ports:
    - name: dummy
      port: 12345
---
kind: StatefulSet
apiVersion: apps/v1beta1
metadata:
  name: csi-attacher
spec:
  serviceName: "csi-attacher"
  replicas: 1
  template:
    metadata:
      labels:
        app: csi-attacher
    spec:
      serviceAccount: csi-attacher
      containers:
        - name: csi-attacher
          image: quay.io/k8scsi/csi-attacher:v0.2.0
          args:
            - "--v=5"
            - "--csi-address=$(ADDRESS)"
          env:
            - name: ADDRESS
              value: /var/lib/kubelet/plugins/linode-blockstorage-csi-driver/csi.sock
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/kubelet/plugins/linode-blockstorage-csi-driver/
        - name: linode-blockstorage-csi-driver
          image: linode/linode-blockstorage-csi-driver:v0.0.1
          args :
            - init
            - "--endpoint=$(CSI_ENDPOINT)"
            - "--token=$(LINODE_TOKEN)"
          env:
            - name: CSI_ENDPOINT
              value: unix:///var/lib/kubelet/plugins/linode-blockstorage-csi-driver/csi.sock
            - name: LINODE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: linodetoken
                  key: token
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/kubelet/plugins/linode-blockstorage-csi-driver/
      volumes:
        - name: socket-dir
          emptyDir: {}
